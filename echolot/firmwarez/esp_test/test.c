#include <stdio.h>
#include <inttypes.h>

int main()
{
	char *ptr = 
	n = sscanf(
        ptr,
        "$GPRMC,%*f,%c,%lf,%c,%lf,%c,%*f,%*f,%d",
        &status,
        &lat_raw,
        &ns,
        &lon_raw,
        &ew,
        &date);



	return 0;
}